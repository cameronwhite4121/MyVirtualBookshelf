<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyVirtualBookshelf.BookshelfPage"
             Title="Shelves">

    <VerticalStackLayout>

        <!-- Shelves Display -->
        <CollectionView ItemsSource="{Binding Shelves}">
            
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical"
                                   ItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="White" 
                            StrokeThickness="2" 
                            BackgroundColor="Transparent" 
                            Padding="5" 
                            StrokeShape="RoundRectangle">
                        
                        <Grid HorizontalOptions="Start" VerticalOptions="Center" Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Button Text="{Binding ShelfName}" 
                                    BackgroundColor="Transparent" 
                                    TextColor="White"
                                    FontSize="24" 
                                    Clicked="OpenShelfBtn_Clicked" 
                                    Grid.Column="0"/>

                            <Label Text="{Binding BookCount, StringFormat='{0} Books'}"
                                   TextColor="White" 
                                   FontSize="18" 
                                   VerticalOptions="Center" 
                                   Grid.Column="1"/>

                            <Label Text="|"
                                   TextColor="White" 
                                   FontSize="30" 
                                   VerticalOptions="Center" 
                                   Grid.Column="2"
                                   Margin="20,5"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>